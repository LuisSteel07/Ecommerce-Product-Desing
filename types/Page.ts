export type Page = {
  actualPage: number; // Este indica la pagina actual
  pages: number; // Este indica la cantidad de paginas que hay
  quantityProducts: number; // Este indica la cantidad de productos por pagina
};
